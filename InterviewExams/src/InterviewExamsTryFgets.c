
#include <stdio.h>
#include <stdlib.h>

/*
int main () {
   FILE *fp;
   char str[60];

   // opening file for reading
   fp = fopen("D:\\Simple Project To Recap\\InterviewExams\\src\\file.txt" , "r");

   if(fp == NULL) {
      perror("Error opening file");
      return(-1);
   }
   if( fgets (str, 60, fp)!=NULL ) {
      // writing content to stdout
      puts(str);
   }
   fclose(fp);

   return(0);
}
*/
